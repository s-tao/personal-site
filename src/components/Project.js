import React, {useState} from 'react';
import { Block, Container } from '../styled.js';

const Project = () => {
    return (
        
      <Container style={{height: 'auto', margin: '5px 0'}}>
        <h1>Project Works</h1>    
      </Container>
    )
}

export default Project;